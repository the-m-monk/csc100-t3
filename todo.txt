the chest is sometimes called block (whoops!), replace all "block" references with "chest"

add dog start variance

make sure that worst/most extreme tinterface reset variants cant a create course with collisions:
    reroll obstacle generatio on collision
    add collision bounding boxes to reset config

add go2 sim camera

policies:
    if distance between dog centre and chest centre < minimum safe distance: decrease fitness rating
    reward model if it visits all obstacles in order:
        draw line between obstacles, reward the closer it sticks to said line

over the course of a simulation, measure these cnn ouputs, the dog state and other relative metrics
add auxillary loss if the arbitrary cnn outputs did not correlate with their respective meaning (ie tunnel distance, etc)

postpone adding swing, model might figure this out on its own (ie turn until the cnn is confident it can see the desired target)
add "post-obstacle" scan function, after clearing obstacle (block, ramp, tunnel) using imu,
    do a 90 degree swing to the left and right, measuring the cnn's output regarding the visbility/detection of the next obstacle
    once swing is complete, orientate the go2 in position of the next obstacle based on the cnn's readings.

model camera might be rotated/incorrect rotation, dump images from sim to test

model may interpret irl grass as the fiish tile, make sure to:
    get the exact irl colour of the finish tile
    and also add grass to the simulator (including avoidance policy)

todo after model proof of concept:

calibrate/model movement variance based on irl testing
calibrate object variance
calibrate object size

select a license for the repository's code and assets 
probably bsd3 or agpl3 for code and some variant of CC for assets

get real go2 camera dimensions

make backdrop random noise/static so the model is incapable of "north-staring" background features,
    although this might cause object recognition hallucinations in "ordered" backdrops due to lack of randomness
    maybe train using a dataset/collection of panorama backdrops? (cities, buildings, streets, etc)

get more tile photos to improve floor texture
record tile size, tiles are currently huge

scene:
ramp (textures)
tunnel (textures)

top image of the chest poorly matches the side images in colour, not super important

chest looks too small, remeasure with a tape measure
ramp to dog ratio looks fine, ramp is maybe a bit large

unitree_webrtc_connect also appears to have some "local odometry" stuffs, only reliable enough for short distances but still very useful



CLARIFICATION STUFFS:
do we actually need to come close to the chest or just avoid it, assuming we just have to avoid it as it will be placed in the way of the green square (probably)